{% extends 'base.html.twig' %}

{% block title %}{% trans %}New Room{% endtrans %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3 mt-5">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">{% trans %}Create new Room{% endtrans %}</h3>
                    </div>
                    <div class="card-body">
                        {{ include('admin_room/_form.html.twig') }}
                    </div>
                    <div class="card-footer text-end">
                        <a href="{{ path('app_admin_room_index') }}" class="btn btn-secondary">{% trans %}Back to list{% endtrans %}</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
