{% extends 'base.html.twig' %}

{% block title %}Edit Room{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4">{% trans %}Edit Room{% endtrans %}</h1>

        <div class="row mt-4">
            <div class="col-md-6">
                {{ include('admin_room/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-6">
                <a href="{{ path('app_admin_room_index') }}" class="btn btn-secondary">{% trans %}Back to List{% endtrans %}</a>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-6">
                {{ include('admin_room/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
